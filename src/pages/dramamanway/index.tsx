import React, { FC } from 'react';
import Table from './table';

const Dramamanway: FC = () => {
    return (
        <div>
            <Table />
        </div>
    );
};

export default Dramamanway;
